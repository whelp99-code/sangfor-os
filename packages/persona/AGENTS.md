<!-- Parent: ../../AGENTS.md -->

# @sangfor/persona — mail-classification seam (stub)

> Persona / hybrid (rule+LLM) mail-classification abstraction. Currently a STUB — `HybridMailClassifier.classifyAsync` returns `{ category: "general" }`.

## Constraints
- Not yet real logic — a reserved seam; do not assume classification works.
- Single-file `src/index.ts`.

## Dependencies
- Depends on: `@sangfor/shared`
- Depended on by: `apps/api`

<!-- MANUAL: Notes below this line are preserved on regeneration -->
