/**
 * U043 quarantine: this reconciliation bypassed scoped Engagement commands.
 * Finance reconciliation must use an authenticated read-only or canonical command.
 */
throw new Error("cfo-deal-reconcile is retired: direct CRM maintenance is not permitted");
