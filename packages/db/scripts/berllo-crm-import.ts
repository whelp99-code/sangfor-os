/**
 * U043 quarantine: this import bypassed scoped CRM commands.
 * Imports require a reviewed authenticated adapter with canonical idempotency.
 */
throw new Error("berllo-crm-import is retired: direct CRM import is not permitted");
