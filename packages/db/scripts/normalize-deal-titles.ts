/**
 * U043 quarantine: this normalization bypassed scoped CRM commands.
 * Deal normalization requires canonical CAS and audit commands.
 */
throw new Error("normalize-deal-titles is retired: direct CRM maintenance is not permitted");
