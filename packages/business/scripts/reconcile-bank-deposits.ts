/**
 * U043 quarantine: this legacy script bypassed scoped CRM commands.
 * Bank reconciliation must be rebuilt on the canonical authenticated API.
 */
throw new Error("reconcile-bank-deposits is retired: direct CRM maintenance is not permitted");
