/**
 * U043 quarantine: this legacy script bypassed scoped CRM commands.
 * Deal reconciliation must be rebuilt on the canonical authenticated API.
 */
throw new Error("reconcile-deal-amounts is retired: direct CRM maintenance is not permitted");
