/**
 * U043 quarantine: this legacy script bypassed scoped CRM commands.
 * Purchase-order reconciliation must use the canonical authenticated API.
 */
throw new Error("reconcile-purchase-orders is retired: direct CRM maintenance is not permitted");
