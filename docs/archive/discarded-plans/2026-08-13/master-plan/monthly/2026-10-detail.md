# M4 상세 실행 계획 (로드맵 08 §6 — 2026-07-11 선행 실행) — 실측 기반

> §2.5 프로토콜 산출물. 정본: 06 문서 + ADR-002(Accepted) + phase-5 실행 문서. 실측: 2026-07-11 정찰(HEAD 98ff9ea). 사용자 지시로 M5까지 선행 개발 — 달력상 10월 배정분을 지금 실행.

## 실측 기준선 (로드맵/06 문서와의 차이)

| 항목 | 문서 가정 | 실측 |
|---|---|---|
| demo-project 리터럴 | "86곳"(08) / "~76곳"(backlog) | **비테스트 29파일/64줄** (+테스트 13파일/33줄). apps/web은 0(기해소) |
| business 재구조화 | 평면 상태에서 시작 | **6/9 폴더 기존재**(domain-ai/crm/finance/governance/orchestration/platform) — 잔여: support/·infrastructure/ 신설 배치, phase14/ 제거, 1-depth 평면 파일 정리 |
| Phase 7-2 고아 ID | 사전 검증 필요 | **고아 0 실측**(4테이블 전수) — 승격 안전. 단 정식 검증 스크립트는 게이트 산출물로 작성 |
| 세금계산서 0행 | "인입 경로 가동 여부 확인" | 배선은 이미 존재(mail-sync가 매 30분 15건 스캔) — **실패 원인 = 사업자번호 미설정**(운영 이슈, 사용자 입력 대기). api쪽 중복 스캔 서비스(scanAndIngestHometaxMails)는 호출자 0인 죽은 코드 — 정리 대상 |
| tRPC 접근 로그 | "접근 로그+grep으로 증명" | **접근 로그 인프라 자체가 없음**(로그의 /trpc 히트는 부팅 배너뿐) |

## Lead 결정 (deviation 기록)

1. **지식그래프 재학습 선행조건 면제**: understand-anything 플러그인 비활성(2026-07-07 비용 최적화) + 재구조화가 이미 2/3 진행돼 폴더 경계가 실코드로 확립됨 + 정찰 보고서·DEV_REFERENCE가 현행 지도 역할. 수동/grep 기반 배치 판단으로 대체하고 면제를 기록한다. (재학습의 원목적 = 폴더 배치 판단 근거 확보 — 이미 확보됨)
2. **D3 소비자 부재 증명 = grep 증거로 대체**: 저장소 전체 tRPC 클라이언트 0 + 단일 운영자 내부 도구로 외부 소비자 존재 근거 없음. 접근 로그 관찰창을 새로 만드는 것은 달력 비용 대비 무익 — grep 증거 + 이 기록으로 갈음, 제거 후 문제 발견 시 revert 가능(라우터 단위 커밋).

## Worker 분해 (Sonnet 5, 파일 스코프 상호 배타)

### W-A: ADR-002 실행 (apps/* 만)
- A1: web tRPC 스텁 6파일+테스트 제거, `openapi.json` 라우트를 REST 표면 기반 정적 스펙으로 교체(**같은 커밋**), `trpc-to-openapi`/`@trpc/*` deps 정리(apps/web package.json)
- A2: apps/api tRPC 제거 — `/trpc` 마운트 해제 → 라우터 단위 커밋(top-level 7개 → cfo/ 16개), 각 커밋에 grep 증거. `trpc.ts`의 procedure 계층은 마지막에. REST(`/api/cfo/*`)는 불변
- Acceptance: 저장소에 `@trpc` import 0, e2e green, openapi.json 200

### W-B: DB 인덱스·FK (packages/db 만)
- B1: 정식 고아 검증 스크립트 `packages/db/scripts/check-engagement-orphans.ts` (LEFT JOIN 4테이블, 게이트 산출물)
- B2: 7-1 복합 인덱스 additive 마이그레이션(Cashflow(type,projectId,date) / Invoice(depositStatus,projectId) / Expense(category,isPaid,projectId) / TaxInvoice(direction,issueDate))
- B3: 7-2 FK 승격 — Invoice/Expense/TaxInvoice.engagementId `@relation`(+ Engagement 역관계) additive 마이그레이션, 스크립트로 고아 0 재확인 선행
- Acceptance: migrate status 무드리프트, cfo:snapshot 선행, 스키마 케이싱 관례 유지

### W-C: 재구조화 잔여 + demo-project 소탕 (packages/business 만)
- C1: phase14/ 제거 + phase13/14 네이밍 정리, 잔여 1-depth 파일을 06 문서 폴더표대로 배치(폴더당 1커밋: git mv+import fix+index.ts), 신규 파일(autonomy-policy/autopilot/watchdog/case-ref) 배치 포함
- C2: demo-project 비테스트 29파일 → `resolveDefaultProjectId/Slug` 주입(async 전파 주의 — 파일별 호출자 시그니처 확인). `recallSemanticFromDb`의 기본 파라미터 포함
- Acceptance: `grep '"demo-project"'` 비테스트 0(시드 제외), src 1-depth엔 index.ts만, golden 무변화, 외부 import 무변화

### W-D (후속, W-A~C 머지 후): Task 4 LLM 계측 + Task 5 멀티테넌시 준비 + api쪽 죽은 스캔 서비스 정리

## M4 종료 게이트
06 문서 게이트 준용 + 02 §4. "지식그래프 재실행" 항목은 위 결정 1로 면제 기록. services CI green 확인.
