# WP-E 증거 (IA 정리) — 2026-07-07

- **PR**: #101 `refactor(ia): consolidate deal entry + mail screens, wire orphan pages` — **머지 2026-07-07T10:44:29Z** (merge commit `22de4b5`)
- **커밋 4개**: dec702f(E-1 딜 단일화) · b2b5741(E-2 메일 통합) · eb16e10(E-3 고아 실사) · cab7c99(config 307)

## 라우트 처리 표
| 라우트 | 처리 | 비고 |
|---|---|---|
| `/opportunities` | redirect → `/deals` | 사이드바 기회 메뉴 제거, 내부 링크 9곳 치환(`href="/opportunities"` grep 0건) |
| `/opportunities/[id]` | redirect → `/deals/[id]` | `/deals/[id]` 기존 정식 상세 사용 |
| `/mail-intelligence` | redirect → `/inbox` | 고유 지표(오늘 승인/변환)는 inbox 통계 패널로 흡수 |
| `/development/mail-candidates` | redirect → `/inbox?tab=candidates` | 후보 관리 뷰를 `components/mail/mail-candidates-list.tsx`로 추출·재사용 |
| `/mail-connection` | redirect → `/settings/mail-connection` | OAuth authorize/callback 목적지 갱신 |
| `/modules`, `/registry` | 유지 | `/settings` 참조 + 실DB 데이터 (감사의 '고아' 판정과 실상 다름) |
| `/cfo/settings` | 유지 + `CFO_NAV` 연결 | 참조 0이었으나 실기능(사업자번호·연동 상태) 보유 → 삭제 대신 연결 |

## Acceptance 검증
- **중복 라우트 0**: 딜 진입점 `/deals` 단일화, 메일 3분산 → `/inbox` 단일 진입 ✅
- **사이드바 메뉴 감소**: 파이프라인(기회) 메뉴 제거, Mail Intelligence/메일 후보 항목이 inbox로 통합 ✅
- **모든 메뉴 실데이터 화면 연결**: 고아 4곳 실사 결과 전부 실참조·실기능 → 유지/연결 ✅
- **e2e/스모크**: `PORT=3103 API_PORT=3202 pnpm test:e2e` → **67 passed / 34 skipped / 0 failed** (변경 전 사전 측정 베이스라인과 동일) ✅
- web typecheck 클린 · lint 0 errors(신규 0) · build 성공 (신규 라우트 포함) ✅

## 발견·해결: redirect() meta-refresh 강등 (Next 16)
`(portal)/loading.tsx`가 만드는 스트리밍 컨텍스트에서 page-level `redirect()`가 HTTP 307 대신 200 + `<meta http-equiv="refresh" content="1;url=...">`로 강등됨(공식 문서에 명시된 동작, 이 리포에서 page-level redirect 첫 사용이라 처음 발견). → `next.config.ts`의 `redirects()`로 config 레벨 리다이렉트 추가(page 스텁은 심층방어로 유지). **curl 실측** (빌드 서버 :3103):

```
/opportunities                → 307 Location: /deals
/opportunities/123            → 307 Location: /deals/123
/mail-intelligence            → 307 Location: /inbox
/development/mail-candidates  → 307 Location: /inbox?tab=candidates
/mail-connection              → 307 Location: /settings/mail-connection
```

검증 서버 종료 영수증: PID kill 확인. e2e 재실행 67 passed 동일.
