# M1 W2~W4 실행·검증 기록 (2026-07-10)

## 실측이 뒤집은 전제 (2.5 프로토콜 작동)

로드맵 08 §3의 W2~W4 항목("Phase 2 잔여 → Phase 3 → Phase 4")은 2026-07-04 기준선이라 낡음. git ancestry 실측 결과 **Phase 2(dedup, PR #85)·Phase 3(11라우트 레이어링)·Phase 4(mail-candidates 분해, b955a19+627a072)는 이미 전부 HEAD에 머지돼 있음**. 실제 잔여 = ADR-002(유일한 미착수) + 잔여 갭 2건.

## W2-1: ADR-002 (산출물: docs/convergence/ADR-002-api-surface.md)

결정: **web = BFF 채택**(마스터플랜 방향), phase-6 문서의 tRPC 도입 방향 폐기. D1 web tRPC 스텁 제거(실행 M4) / D2 OpenAPI는 REST 기반으로 유지 / D3 apps/api tRPC는 소비자 부재 증명 후 제거(실행 M4) / D4 Phase 7 = 인덱스+FK 승격만(segment/riskScore 기추가분 수용, 추가 예측 컬럼은 07 전 금지).

결정 인풋 실측(전부 이 세션 직접 측정): web REST 97 route · web tRPC = hello 스텁 6파일, `.tsx` 소비자 0 · 저장소 전체 tRPC 클라이언트 import 0 · raw HTTP `/trpc` 소스레벨 호출 0(검색 히트는 스텁 자신의 .next 산출물뿐) · `openapi.json`은 스텁 종속 · segment/riskScore는 2026-07-03 기추가 · 마스터플랜 7-1 인덱스 부재.

phase-6 문서 상단에 "ADR-002로 대체/수정됨" 배너 추가(acceptance 문구 일치). **Status: Proposed — 사용자 승인 대기**(승인 시 Accepted 전환, 03 문서 Task 0 Step 4).

## W2-2: Phase 3 잔여 갭 — dashboard/[role] 레이어링 완결

`apps/web/src/app/api/dashboard/[role]/route.ts`의 prisma 직접 호출 12건(8개 per-role 헬퍼)을 신규 `packages/business/src/role-dashboard-data.ts`로 그대로 이동(opencode-coder 위임, diff 직접 리뷰). 라우트 127→22줄, prisma 참조 0. `role-dashboard.ts`(순수 계산)는 미변경.

**행동 보존 증명**: 실행 중 dev 서버에서 8개 role + unknown(404) 응답을 변경 전/후 캡처 → **9개 전부 바이트 단위 동일**. typecheck(business/web) clean, role-dashboard.test.ts 9/9 무회귀.

## W2-3: Phase 2 잔여 미세 갭 — 죽은 헬퍼 제거

`classify-rules.ts`의 `extractCompanyFromDomain`(스테일 `berlo.co.kr`/`berlo.com` 라벨 포함)·`extractContactFromEmail` — 저장소 전체 호출자 0, 테스트 참조 0 확인 후 제거. typecheck clean, business 572 테스트 통과, golden 무변화.

## W3: 02 검증서 게이트

| 게이트 | 결과 |
|---|---|
| G1 lint | exit 0 (0 errors) — 수정 후 재실행 포함 |
| G1 typecheck | exit 0 전 패키지 |
| G2 unit | 844 passed 전체 / business 572+통합2 |
| G3 integration | `CI_INTEGRATION=1` convert 테스트 2/2 (실DB) |
| G4 build | exit 0 |
| golden | `mail-candidates.golden.test.ts` 무변화 |
| 시나리오 1 (라이브 재연) | ✅ 아래 |

**시나리오 1**: 이중게이트 통과 후보 1건(`cmr22jdev...`, confidence 85 + approve_candidate) → `POST /api/mail-candidates/batch` 승인(count=1) → `POST /api/mail-candidates/convert` → `work_tasks` 2→3, 제목 일치 신규 태스크 생성 확인.

### 검증 루프가 잡은 실버그 (수정 완료)

시나리오 1 검증 중 전환된 후보의 `created_entity_id`가 비어 있는 것을 발견 → 추적 결과 `mail-candidates-convert.ts`의 **task 전환 분기만 `createdEntityType/createdEntityId` 미설정**(opportunity/customer/partner 분기는 설정함). M2 백로그 "converted 후보 위생"이 과거 데이터 문제가 아니라 현행 코드의 살아있는 버그였음.

- 수정: task 분기도 신규/기존 양쪽에서 linkage 설정 (`mail-candidates-convert.ts`)
- 회귀 테스트: `mail-candidates-convert.test.ts`에 task 케이스 추가(신규 생성 + 중복 재사용 linkage), CI_INTEGRATION=1 실DB 2/2 통과
- 데이터 백필: 댕글링 converted 후보 3건(task 1 + opportunity 2, 전부 정확히 1개 엔티티 매칭) 링크 → 댕글링 0, 3건 모두 title 대조 검증 통과

## 잔여

- ADR-002 사용자 승인 → Accepted 전환
- W4: 상태 문서 동기화(00-INDEX §2, 03 기준선, 08 §3 체크박스) + M1 종료 기준 판정
