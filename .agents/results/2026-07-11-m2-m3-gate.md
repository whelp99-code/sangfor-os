# M2/M3 마감 게이트 기록 (2026-07-11)

## M2 종료 기준 판정 (로드맵 08 §4)

1. **결정 로그 우회 경로 0** ✅ — `domainDecisionLog.create` writer는 `recordDecision`(ai-decision.ts) 1곳뿐(grep 검증). recordDomainDecision은 티어게이트 위임 래퍼로 전환(#117), 메일 생명주기 9곳 스파인 이관+policy-decision-log writer 삭제(#119). policy/agent 감사 스트림은 ADR-001 D2가 의도적으로 유지(문서화됨).
2. **partners ≥ 40행(실데이터)** ✅ — 3→49행(#114, ground-truth 재분류 47사 + 기존). 레지스트리 코드 영속화로 재유실 불가.
3. **후보 정밀도 KPI 개선** — **정직 판정: 지표 하락(51.0→38.0%)이며 이는 개선 실패가 아니라 정크 청소의 통계 반작용**(재검증이 정크 118건 추가 거부 + 아티팩트 전환 4건 rejected 환원 → 분모 증가, 분자 불변). 이 지표는 큐의 역사적 구성을 재므로 청소가 낮춘다. 개선의 실질 증거는 유입단 차단(아티팩트 필터 — 신규 생성 0 검증)과 레지스트리 기반 분류 강화이며, 수치 개선은 **신규 유입분 기준으로 다음 주기 kpi-weekly에서 재측정**.

## M2 라이브 재연 3종 (04 문서 게이트)

| 재연 | caseRef prefix | 결과 |
|---|---|---|
| mail approve→전환 | `mail_candidate:` | ✅ W3 시나리오1 (2026-07-10, work_task 생성 검증) |
| CRUD edit → 스파인 1행 | `opp:` | ✅ 라이브 PATCH → (human, entity_edit, corrected, T0) 1행 — 검증 중 **updateOpportunity만 actor:'sales' 잔존 불일치 발견·통일**(신규 4곳은 human이었음) |
| hub decision | `eng:` | ✅ 합성 pending ai_proposal → recordHumanDecision → (human, human_review, T0) + pending resolved by human — test 데이터 클린업 완료 |

## M2 잔여/이월

- **B-4 커맨드바 실무 플레이북**: 미착수 이월 — 플레이북 저장소가 인메모리 싱글턴(재시작 소실)에 read-only 도구 6종뿐임을 실측. 영속화(DB 모델)+비즈니스 액션 도구 화이트리스트 확장은 autopilot 안전장치와 함께 설계 필요 → M3 후속/M4.
- **customers 오염 51행**: 사용자 결정 대기(backlog).
- **poc 로그 오염 136행 백필**: dry-run 리포트만(#119), 실행은 별도 결정.

## M3 태스크 랜딩 (05 문서)

| Task | 상태 |
|---|---|
| 1 AutonomyPolicy+resolveAutonomyMode | ✅ #118 (TDD 19케이스, observe 시드) |
| 2 runAutopilotPass | ✅ #123 (DI 10테스트, 연속3회 뒤집힘 자동강등, 라이브 dryRun 검증) |
| 3 파이프라인 스케줄 | ✅ #124 — **실측 pivot**: agent-schedules는 인메모리+cron불가+tick=LLM루프라 구조적 부적합 → launchd 4잡(sync 30분/classify 매시/autopilot 매시/브리핑 평일08:00), 4 엔드포인트 라이브 검증, **2026-07-11 로드 완료(가동 중)** |
| 4 일일 브리핑 | ✅ #124 (실쿼리+LLM 요약+결정론 폴백 — 폴백 경로 라이브 검증. 스키마 실측 편차 3건 문서화: SupportCase에 createdAt 없음 등) |
| 5 와치독 | ✅ #124 (리뉴얼 D-버킷/SLA/CFO 에스컬레이션, 라이브 idempotency 증명. 해결-SLA는 스키마에 필드 부재로 미구현 명시) |
| 6 관제 화면+kill-switch | ✅ 이번 PR (ai-team 페이지 관제 섹션, DB-backed kill-switch(env 하드오프 우선), 토글→스파인 기록 라이브 왕복 검증, Playwright 스크린샷 확인) |

## M3 종료 기준 — 관찰 대기 항목 (달력 종속)

- **저위험 1종 자동화 가동**: 코드·정책·스케줄 전부 준비, **정책이 observe 시드 상태이므로 실제 auto 전환은 자율도 표본(samples≥10) 축적 후 mode='auto' 승격 결정 필요** — 사람 검토 루틴(§10 상시)이 표본을 쌓는다.
- **뒤집힘율 <5%**: auto 가동 후에만 측정 가능.
- **kill-switch 실훈련**: Task 6 검증에서 라이브 토글 왕복(off→on) + 스파인 기록 확인 — 단 auto 가동 중 정지 훈련은 auto 승격 후 재실시 권고.
- **24h 무인 가동**: launchd 4잡 2026-07-11 로드 — 익일 로그(`.agents/results/kpi/*.launchd.log`)로 판정.
