# WP-A Task A-3 Step 3 — 라이브 브라우저 검증 (2026-07-07)

판정: **PASS**

승인 → brass 링크 노출 → 클릭 → 문서 v1 본문 렌더, 콘솔 에러 0건, generated_documents에 approved 신규 row 생성 — VERIFY 조건 전부 충족.

- 환경: web http://localhost:3101 (dev, 재기동 없음), postgres :5434, 브랜치 fix/color-gate-calibration (전환 없음)
- 도구: Playwright CLI 1.61.1 (`node_modules/.bin/playwright test`, headless chromium) — playwright-verify 스킬 방식, MCP 미사용
- 결과: `1 passed (9.1s)`

## 1. 수행 단계 로그

1. 사전 조사 — pending 제안 탐색: `domain_decision_logs`에서 `decision_type='ai_proposal' AND outcome IS NULL` 8건 확인. 대상으로 최신 건 선택: 제안 `cmr9udczn00059kdgu5jcg7he` ("씨젠 상포 제안서 개요(보완본)", presales), engagement `cmr6cej5z00159k2ytjk2dhuz` (씨젠 - Sangfor 도입). pending이 이미 있어 `/api/projects/[id]/generate` 호출(LLM 생성)은 불필요했음.
2. 승격 체인 사전 확인 — 해당 engagement는 opportunity `cmr6cej5u00139k2yznsf12ti` → project `cmr0v0ka800009kcconlrgher` 체인 완비 (promotion skip 조건 아님).
3. 승인 전 baseline — `generated_documents` 최신 row: `cmr9uvuc4000u9k124f4i9rnj` (2026-07-06 23:33).
4. Playwright 플로우 실행 (스펙 /tmp/wp-a3-verify/wp-a3-verify.spec.ts, 실행 후 삭제):
   - `/projects` 목록 진입 → 스크린샷 01
   - `/projects/cmr6cej5z00159k2ytjk2dhuz` 허브 진입 → h1 "씨젠 - Sangfor 도입", 프리세일즈 레인에 "AI 제안 · 검토대기" 제안 2건 + 컬러게이트 5렌즈 칩(B/R/O/G/T, 컬러 검증 보류) 렌더 확인 → 스크린샷 02
   - 프리세일즈 레인 `승인` 버튼 클릭 → "기록됨" 표시 + brass 링크 "산출물 문서 보기 →" 노출. `href=/proposals/cmr9y8j9k00069kvrotvskw9f`, computed color `rgb(124, 94, 30)` = #7C5E1E (brass) 확인 → 스크린샷 03/03b. router.refresh 후 승인된 제안이 검토대기 큐에서 빠지고 산출물 목록에 "씨젠 상포 제안서 개요(보완본) approved"로 이동한 것도 03에서 확인.
   - brass 링크 클릭 → `/proposals/cmr9y8j9k00069kvrotvskw9f` 랜딩. 제목 h1 "씨젠 상포 제안서 개요(보완본)", 배지 domain-ai / approved / Customer: 씨젠, "문서 편집"에 마크다운 본문, "버전 이력 (1)"에 버전 1 본문 렌더 확인. v1 본문 헤드: `# 1. 요약 | 씨젠은 OCP 이슈와 영구라이선스 전환 불확실성으로...` (본문 길이 assert > 100자 통과) → 스크린샷 04
5. DB 이중확인 (읽기 전용) — 아래 §3.
6. 콘솔 에러 캡처 — 아래 §4.

## 2. 스크린샷 (.agents/results/wp-a3-screens/)

| 파일 | 내용 |
|---|---|
| 01-projects-list.png | /projects 목록 |
| 02-hub-presales-pending.png | 허브 전체 — 프리세일즈 레인 pending 제안 + 컬러게이트 칩 (승인 전) |
| 03-lane-approved-brass-link.png | 승인 직후 프리세일즈 레인 — "기록됨" + brass "산출물 문서 보기 →" 링크 |
| 03b-hub-after-approve-full.png | 승인 직후 허브 전체 |
| 04-proposal-document-v1.png | /proposals/cmr9y8j9k00069kvrotvskw9f — 문서 v1 본문 렌더 |

## 3. DB 확인 쿼리 + 결과

```sql
SELECT id, status, engagement_id FROM generated_documents ORDER BY created_at DESC LIMIT 3;
--            id             |  status  |       engagement_id
-- cmr9y8j9k00069kvrotvskw9f | approved | cmr6cej5z00159k2ytjk2dhuz   ← 이번 승인으로 신규 생성
-- cmr9uvuc4000u9k124f4i9rnj | approved | cmr6cej5z00159k2ytjk2dhuz
-- cmr9uv9m9000j9k12zxzkiqku | approved | cmr6ce30h000m9k2y5228r941

SELECT generated_document_id, version, length(body_markdown) FROM document_versions
 WHERE generated_document_id='cmr9y8j9k00069kvrotvskw9f';
-- cmr9y8j9k00069kvrotvskw9f | 1 | 2042   ← v1, 원본 제안 bodyMarkdown(2042자)과 길이 일치

SELECT id, outcome, resolved_by, resolved_at FROM domain_decision_logs
 WHERE id='cmr9udczn00059kdgu5jcg7he';
-- cmr9udczn00059kdgu5jcg7he | approved | human | 2026-07-07 01:07:13.913   ← pending 제안 resolved
```

## 4. 콘솔 에러

0건. 스펙이 전 플로우(허브 진입 → 승인 → 링크 클릭 → 문서 페이지)에 걸쳐 `pageerror` + `console.type()==='error'`를 수집해 `toHaveLength(0)` assert — 통과.

## 5. 엣지 케이스 (documentId 없음 → 무링크·무에러)

자연 발생하지 않음. 승인한 제안은 title+bodyMarkdown과 engagement→opportunity→project 체인이 모두 있어 승격이 정상 수행됨. 계획서상 일부러 만들 필요 없음 — 미기록.

## 6. Cleanup 영수증

- /tmp/wp-a3-verify/ (스펙 + node_modules 심링크) 삭제 완료
- 제품 코드 수정 0건, dev 서버 유지, DB 쓰기는 UI 승인 액션(제품 정상 플로우)뿐
