# WP-D (화면 정직성) 라이브 브라우저 검증 — 2026-07-07

- Worktree: `/Users/jmpark/Playground/sangfor-os/.worktrees/wp-d-screens`
- Branch: `fix/screen-honesty` (origin/main 리베이스 완료, 4 commits ahead)
  - `4ba6e34` feat(web): agent-run command client for AI workspace command bar
  - `b6ed84f` feat(web): wire six workbench AI assistants to /api/agent/run
  - `7cd09b1` fix(web): remove dishonest instruments — fake security feed + coming-soon stubs
  - `0154abd` fix(web): honest deals header — reflect active filter, drop fake dropdown caret
- Stack: web :3101, api :3200, postgres :5434 (docker, pre-existing), 9router :20128 (pre-existing, reachable, `/v1/models` → 200)
- Verifier tool: Playwright CLI 1.61.1, headless, config `/tmp/wp-d-playwright.config.ts`, spec `/tmp/wp-d-qa/wp-d-verify.spec.ts`

## Setup notes (gotchas hit this run)

- `packages/db/.env` and `apps/web/.env.local` were missing in the worktree (known gotcha) — copied from the main repo.
- `pnpm --filter @sangfor/shared build` / `@sangfor/config build` had stale-looking `dist/`; rebuilt anyway.
- **New gotcha this run**: `apps/api` crashed on boot (`Cannot find module '.../@sangfor/auth/dist/index.js'`) because `packages/auth` (and several sibling packages: api-utils, health, infra, persona, ui) had no `dist/` in this worktree. Fixed with `pnpm -r --filter "./packages/**" build` (packages using `src` directly via `exports` — agent/business/mail-intelligence — only need their `--noEmit` typecheck, no dist required). Worth folding into the dev-reference gotcha list: **any worktree needs a full `pnpm -r --filter "./packages/**" build` before `dev-up.sh`, not just shared/config.**

## Verdict table

| # | Item | Verdict | Evidence |
|---|------|---------|----------|
| 1 | AI command bar (`/sales`) → `/api/agent/run` real call, honest result/failure text | **PASS** | `1-agent-run-network.json`: POST `/api/agent/run` → 200, `text/event-stream`; toast text = `"실행 실패: agent_run_failed"` (never "준비 중"). Screenshots `1-sales-before.png`, `1-sales-after-toast.png`. |
| 2 | `/security` — no crash, no fake feed, real stat tiles, 0 console errors | **PASS** | Screenshot `2-security.png`: activity feed shows "활동 로그가 없습니다.", 4 stat tiles (권한 변경/특권 접근/감사 무결성/AI 정책 위반) render server-derived values. `2-console-errors.json` = `[]`. |
| 3 | Stub removal: `/deals`, `/contacts`, `/customers`("companies"), no "복사 (준비 중)" | **PASS** | See per-page detail below. |
| 4 | Regression smoke (`scripts/dev-smoke.sh`) | **PASS** | All 10 web routes 200/ok, both API dev-bypass routes 200, `✅ smoke passed`. |
| 5 | Full-page console error sweep (17 routes) | **PASS — 0 errors** | `5-full-sweep-console.json`: every route → `[]`. |

**Overall: PASS on all VERIFY criteria (1, 2, 3, smoke, dev-down).**

### Item 3 detail

- `/deals`: no "필터" / "열 설정" buttons (`getByRole('button', …)` count 0). Header defaults to "전체 · 67건"; clicking the "제안" chip updates it live to "제안 · 50건" — label and count track the active filter (screens `3a-deals-default.png`, `3a-deals-chip-filtered.png`). Row `⋯` dropdown shows only 편집/삭제 — no "복사 (준비 중)" (`3a-deals-row-menu.png`).
- `/contacts`: no 필터/열 설정 buttons; empty state "연락처가 없습니다." with no fake rows (`3b-contacts.png`).
- `/customers` (renders `CompaniesWorkspace`, the task's "/companies"): no 필터 button (`3c-customers.png`).

## Non-blocking finding (out of scope for this branch, flagging for follow-up)

The `AICommandBar` toast (`apps/web/src/components/ai-workspace/ai-command-bar.tsx`) renders correct text in the DOM (confirmed via `textContent`) and reaches `opacity: 1`, but is visually **occluded by the sibling "AI 활동 로그" card** above it — only a ~2px sliver is visible at default viewport size (screenshots + a throwaway stacking probe confirmed `parentOpacity: 1` yet the toast box is painted behind the activity-feed card). Verified via `git log` that this toast/layout code predates this branch (last touched at `67e0f53`, well before `fix/screen-honesty`); the 4 commits under test only changed `onSend`'s return-type contract, not this positioning. Not a regression from WP-D and not counted against the PASS verdict, but real users on `/sales` (and the other 5 wired workbenches) would not actually see the toast pop up — worth a separate fix ticket for the z-index/stacking of the toast wrapper (`position: absolute; bottom-full` inside a `relative` Card, competing with a non-positioned sibling Card above it).

## Cleanup

- `scripts/dev-down.sh` run — ports 3101/3200 freed.
- `/tmp/wp-d-qa/`, `/tmp/wp-d-playwright.config.ts` removed after run.
- No DB writes made by the verifier beyond the one `agent_run` log record created by exercising the command bar (expected product behavior, in-scope per task SCOPE).
