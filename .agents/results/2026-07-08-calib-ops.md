# 2026-07-08 캘리브레이션 운영 마감 (§9 / M1-1)

## 1. 이중게이트 배치 승인·전환

**사전 확인**: `confidence>=85 AND proposed AND metadata.aiRevalidation.decision='approve_candidate'` 쿼리로 12건 확인 — 로드맵 §9 예상치와 일치.

**실행**:
```
POST /api/mail-candidates/batch {"action":"approve","minConfidence":85}
→ {"success":true,"action":"approve","count":12}

POST /api/mail-candidates/convert
→ {"success":true,"customersCreated":0,"partnersCreated":0,"customersSkipped":0,
   "partnersSkipped":0,"customersMerged":0,"partnersMerged":0,
   "opportunitiesCreated":12,"tasksCreated":0}
```

**전후 분포** (`mail_derived_candidates`, status별 count):

| status | 전 | 후 |
|---|---|---|
| proposed | 964 | 952 |
| converted | 171 | 183 |
| rejected | 176 | 176 |
| knowledge_only | 42 | 42 |

proposed -12 / converted +12로 정확히 상쇄. `opportunities` 테이블에 최근 5분 이내 생성분 12건 확인(DB 직접 카운트).

## 2. 폴백 529건 재검증

**블로커**: `cx/gpt-5.4-mini`(원래 캘리브레이션에 쓰인 모델) 쿼터가 24시간+ 429 지속(9router 자체는 정상, 모델별 사용량 한도). 사용자 확인 후 **9router의 다른 모델로 임시 대체**하기로 결정.

**모델 선정 과정**: `kr/claude-sonnet-4.5`는 응답을 마크다운 코드펜스(```json)로 감싸 파싱 실패(`Unexpected token`). `kr/deepseek-3.2`는 대체로 클린 JSON을 반환하지만 비결정적으로 가끔 펜스를 씌움 — 근본 수정으로 `packages/business/src/mail/classify-ai.ts`의 `callLlmRevalidation`에 `stripJsonCodeFence()` 추가(어느 모델이든 방어). dev-clean 브랜치와 wp-calib 워크트리(`fix/classifier-calibration`) 양쪽에 동일 적용.

**실행**: `calib-run-fallback.local.ts`(force 미사용, 763cd95 자가치유 캐시가 fallback 태그된 건만 자동 재시도)를 `OPENAI_MODEL=kr/deepseek-3.2`로 4회 재시도 루프 실행 — 매 회 여전히 fallback인 id만 추려 다음 회차 대상으로.

| 회차 | 대상 | 성공 | 폴백 | 실패 사유 |
|---|---|---|---|---|
| 1 | 529 | 245 | 284 | `openai_http_403`(일시적, 자연 해소) |
| 2 | 284 | 189 | 95 | 동일 |
| 3 | 95 | 75 | 20 | 동일 |
| 4 | 20 | 20 | 0 | — |

**최종**: 원 529건 전량 DB 재확인 결과 fallback 0건(`metadata.aiRevalidation.fallbackReason is not null` 카운트 0). §9-2 완료.

**품질 캐리비트 — 중요**: 대체 모델(`kr/deepseek-3.2`)은 원 캘리브레이션 모델(`cx/gpt-5.4-mini`, PR #105 A/B 4라운드 튜닝)과 신뢰도 산출 분포가 다름. `approve_candidate` 판정 150건 중 confidence≥85(이중게이트 통과선)는 단 1건, 145건이 70~84 구간 — 대체 모델이 구조적으로 더 보수적인 confidence를 냄. **이 529건은 사람 검토(`needs_human_review`) 큐로 정상 유입되며 데이터 훼손은 아니지만, cx 쿼터가 풀리면 원 모델로 재검증해 이중게이트 신뢰도 기준과의 정합성을 재확인하는 것을 권장**(M2 이후 트랙 또는 별도 후속).

## 3. Final Wave 판정

**최종 큐 분포** (`mail_derived_candidates`, status):

| status | §9-1 후 | Final |
|---|---|---|
| proposed | 952 | 840 |
| converted | 183 | 183 |
| rejected | 176 | 290 |
| knowledge_only | 42 | 44 |

proposed 952→840(-112)은 재검증 중 `decision=reject`가 자동으로 `status=rejected`를 트리거한 결과(rejected +114, knowledge_only +2 — 거의 정확히 상쇄, 차액은 동시성 처리 경계값).

**proposed 내 재검증 결정 분포**: `needs_human_review` 685 · `approve_candidate` 150 · 미분류 5(소수 잔차, 후속 조사 대상). 이중게이트(confidence≥85 AND approve_candidate) 즉시 통과 가능 1건 — 위 품질 캐리비트 사유로 낮음, 대부분 사람 검토 큐(685건)로 정상 유입.

**잔여 fallback**: 이번 배치 대상 529건 외에 전체 proposed 중 9건 별도 잔존(원 배치 범위 밖, 소규모 — 후속 데일리 배치(M1-5)가 자연 흡수).

**판정**: 플랜 §9 1~3 및 M1-1 완료. boulder `classifier-calibration-2026-07-07` → completed.
