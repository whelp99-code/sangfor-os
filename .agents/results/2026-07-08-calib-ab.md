# 메일 분류기 캘리브레이션 A/B 검증 (2026-07-07~08)

브랜치 `fix/classifier-calibration` (커밋 4: 56cd3c9 신호점수 · 42a0a31 재검증확대+블렌딩 · 763cd95 캐시픽스 · 9c0d132 프롬프트하드닝+이중게이트).
층화 샘플 60건 (customer 20[known_match 5·vendor_garbage 4] / partner 10 / opportunity 15 / task 10 / poc 5), 백업 `/tmp/calib-backup-2326.sql`(4.7MB).

## 라운드 요약
| 라운드 | 조건 | 결과 |
|---|---|---|
| 1차 | opencode.ai zen | 60/60 폴백 — **zen 월간 한도 429**(GoUsageLimitError) 발견 |
| 2차 | 9router 전환 | 60/60 폴백 — **아웃티지 폴백이 metadata 캐시에 영구 고정**되는 제품 결함 발견 → `763cd95` (force 옵션 + fallbackReason 캐시 무효화) |
| 3차 | force+9router | 폴백 0/60, LLM 실동작. **anti-gaming FAIL**: 팝빌(llm 92)·Example/Mails(87-93)·자사 Sangfor(86-90) 고득점, ≥85가 25/60 — 원인: 재검증 프롬프트에 ground-truth/레지스트리 부재 + 배치승인 confidence 단독 게이트 → `9c0d132` |
| 4차 | 하드닝 프롬프트 | **판정 PASS** (아래) |

## 4차 최종 지표 (60건)
- 폴백 1/60 (1.7%) — 기준 ≤30% ✅
- decision 분포: needs_human_review 41 / **reject 14** / knowledge_only 4 / approve_candidate 1
- **Anti-gaming ✅**: 파서 아티팩트(Example×7, Mail/Mails, `<1 min`)·팝빌 전원 reject(status=rejected, 큐 영구 퇴출). **이중 게이트(≥85 AND approve_candidate) 통과 정크 0건**
- **Recall ✅**: 진성 known-match(Gsenc×2) → 90/85, 기존 고객과 dedup 감지되어 needs_human_review(병합 검토가 정답) — 큐 상위 랭크
- 샘플의 30%(18/60)가 자동 정리(reject+knowledge) — 전량 적용 시 유사 비율 기대

## 잔여 관찰 (backlog)
- LLM이 confidence를 "결정 확신도"로 해석하는 경향(reject인데 94 등) — 실害 없음(rejected는 큐 이탈, 배치는 decision 게이트), 05 자율도 게이트로 대체 예정
- "Partner: Sangfor Tech Support"(자사) 4건 중 2건이 reject 대신 review 잔류 — 안전하나 프롬프트 반복 개선 여지
- 상류 생성기가 만드는 아티팩트 후보(Example류)는 생성 단계 필터가 근본 해법

## 게이트 (출하 전, `.agents/results/2026-07-07-calib-gates.md`)
CI 실순서(패키지 선빌드) 기준 5/5 green — lint 0 err / typecheck 클린 / business 554 passed / build OK / CI_INTEGRATION 582 passed. 최초 실행의 lint·typecheck·test 실패는 워크트리 dist 미빌드 함정(브랜치 무관, DEV_REFERENCE 기록된 gotcha).

## 운영 (전량 재검증)
proposed 1,120건 대상 force 재검증 nohup 실행 (PID 19448, `/tmp/calib-full-run.log`, 결과 `/tmp/calib-results.jsonl`). 종료 후 전후 분포는 별도 증거로 추가.
