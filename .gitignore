node_modules
dist
.next
.turbo
coverage
test-results
allure-results
*.tsbuildinfo
.env
.env.local
.env.production
.env.*.local
__pycache__
.DS_Store
*.db
.worktrees/

# Tooling/session caches (oma, serena MCP)
.omo/
.serena/
.superpowers/

# Derived mail-learning snapshots (redacted but mail-derived; not for VCS history)
memory/mail-learning/
memory/agent-handoffs/
.understand-anything/
.local-prod/

# Local DB snapshots + replay run artifacts (large / mail-derived; not for VCS)
.agents/backups/
.agents/results/replay/
.agents/results/kpi/*.launchd.log
.agents/results/kpi/*.launchd.err.log
.agents/results/kpi/watchdog-status.json
.env.bak*

# oma docs generated artifacts
docs/generated/

# Local DB backups (real data — never commit)
.local-backups/
